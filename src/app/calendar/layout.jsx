export const metadata = {
  title: "Calendar — JobFlow Career OS",
  description:
    "Interviews and reminders laid out month by month across your active pipeline.",
  openGraph: {
    title: "Calendar — JobFlow Career OS",
    description: "See every interview and reminder in one editorial month grid.",
  },
};

export default function CalendarLayout({ children }) {
  return <>{children}</>;
}