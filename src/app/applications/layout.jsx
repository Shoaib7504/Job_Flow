export const metadata = {
  title: "Applications — JobFlow Career OS",
  description:
    "Every opportunity as an editorial dossier: stage, source, salary and momentum.",
  openGraph: {
    title: "Applications — JobFlow Career OS",
    description: "Filter, sort and track every application in one structured index.",
  },
};

export default function ApplicationsLayout({ children }) {
  return <>{children}</>;
}