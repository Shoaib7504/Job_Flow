export const metadata = {
  title: "Pipeline — JobFlow Career OS",
  description:
    "The opportunity map: drag applications across saved, applied, screening, interview, offer and accepted.",
  openGraph: {
    title: "Pipeline — JobFlow Career OS",
    description: "A journey board for every opportunity, from saved to accepted.",
  },
};

export default function PipelineLayout({ children }) {
  return <>{children}</>;
}